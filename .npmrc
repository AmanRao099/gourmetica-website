# Next 16 requires Node >=20.9.0. Without this, npm treats the `engines` field
# in package.json as advisory: install succeeds on an old Node and the failure
# surfaces later as an opaque "build worker exited with code: 1". Fail at
# install instead, with a message that names the actual problem.
engine-strict=true
