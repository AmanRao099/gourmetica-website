# Label

The `Label` component is used to label form fields. It defaults to the `label` HTML tag and applies appropriate form weights.

## Props
- `size`: `default` (body-sm) or `large` (body).
- Accepts all standard `label` attributes, notably `htmlFor`.
