# Eyebrow

The `Eyebrow` component is a small uppercase typographic element used above main headings to provide context or category labels (e.g. "LATEST NEWS", "SPECIAL OFFER").

## Props
- `color`: `brand` (default), `neutral`.
- `size`: `sm`, `md` (default).
- `as`: Defaults to `span`.
