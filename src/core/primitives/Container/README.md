# Container

The `Container` primitive is the **only** horizontal width constraint allowed in the system. Never use `max-w-7xl` or `max-w-5xl` randomly in pages; wrap page contents in a `Container` instead.

## Props
- `size`: `sm` (3xl), `md` (5xl), `lg` (7xl default), `full` (100%).
