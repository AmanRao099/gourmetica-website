# Grid

The `Grid` primitive provides a strongly typed CSS Grid layout constraint.

## Props
- `columns`: Number of columns (1, 2, 3, 4, 5, 6, 12).
- `gap`: Spacing token (`none`, `sm`, `md`, `lg`, `xl`).
