# Stack

The `Stack` primitive is a convenience wrapper around `Flex` that defaults to a vertical column direction. 

## Usage
Use `Stack` whenever you need to stack elements vertically. Use `Flex` for horizontal rows.

## Props
Inherits all `Flex` props.
- `direction`: Defaults to `col`.
