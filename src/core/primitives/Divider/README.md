# Divider

The `Divider` primitive separates content visually and structurally.

## Props
- `orientation`: `horizontal` (default) or `vertical`.

## Accessibility
The `Divider` automatically applies `role="separator"` and the correct `aria-orientation` attribute.
