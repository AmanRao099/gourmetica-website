# Input

The `Input` component allows users to enter and edit text. It wraps the native `<input>` element and supports standard HTML attributes.

## Props
- `size`: `sm`, `md` (default), `lg`.
- `hasError`: Applies error styling and sets `aria-invalid="true"`.
