# Textarea

The `Textarea` component allows users to enter and edit multi-line text.

## Props
- `hasError`: Applies error styling and sets `aria-invalid="true"`.
- Supports all standard `<textarea>` attributes like `rows`.
