# Checkbox

The `Checkbox` component is built on top of `@radix-ui/react-checkbox` for WAI-ARIA compliance.

## Usage
Always pair with a `<Label htmlFor="some-id">`.

## Props
- `hasError`: Highlights the checkbox in red.
- Supports all Radix UI Checkbox props.
