# ButtonGroup

The `ButtonGroup` groups related buttons together, removing duplicate borders and adjusting corner radii seamlessly using Tailwind child selectors.

## Props
- `orientation`: `horizontal` (default) or `vertical`.
- Inherits `Flex` props (except `gap`).
