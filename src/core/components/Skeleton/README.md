# Skeleton

The `Skeleton` component is a placeholder to show a loading state for content.

## Usage
Provide `width` and `height` dimensions via Tailwind classes (e.g., `w-full h-8`) or inline styles to match the content it replaces.

## Props
- Inherits all `Box` props.
