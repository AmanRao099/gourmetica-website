# Switch

The `Switch` component is built on top of `@radix-ui/react-switch` for WAI-ARIA compliance.

## Usage
Always pair with a `<Label htmlFor="...">`.

## Props
- Supports all Radix UI Switch props.
