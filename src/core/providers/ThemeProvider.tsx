'use client';

import React from 'react';

/**
 * Standard Theme Provider placeholder.
 * Will be extended with next-themes or context logic as needed.
 */
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
