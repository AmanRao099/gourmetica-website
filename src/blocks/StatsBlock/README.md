# StatsBlock

The `StatsBlock` displays a collection of statistics, either inline or split alongside a descriptive text block.

## Props
- `title` (string): Optional headline
- `description` (string): Optional paragraph
- `stats` (Array): Array of `{ label, value }`
- `layout` (`split` | `inline`)
- `theme` (`light` | `dark` | `brand`)
